{
  timestamp: string;        // ISO 8601 timestamp of the click
  ipAddress: string;        // Requesting IP address
  referrer: string;         // Referer header from the request
  location: {               // Coarse-grained location
    country: string;
    region: string;
    city: string;
  } | null;
}